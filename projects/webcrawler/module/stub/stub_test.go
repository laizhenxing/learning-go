package stub

import (
	"testing"
)

var addStr = "127.0.0.1:8080"

//var mid = module.MID("D1|" + addStr)

func TestNewModuleInternal(t *testing.T) {
	//id, err := NewModuleInternal(mid, module2.CalculateScoreSimple)
}